import React from "react";

import { Container, Header, Main, Footer } from "@components";

const Home: React.FC = () => {
    return (
        <Container>
            <Header />
            <Main />
            <Footer />
        </Container>
    );
};

export default Home;
